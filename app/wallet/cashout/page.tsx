"use client";

export default function CashoutDisabled() {
  return (
    <main style={{padding:24}}>
      <h1 style={{fontSize:20, fontWeight:700}}>Cash-out temporarily disabled</h1>
      <p style={{marginTop:8}}>
        This route is intentionally stubbed to complete deployment. We’ll re-enable once maps & payouts are finalized.
      </p>
    </main>
  );
}
